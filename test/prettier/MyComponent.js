import React from 'react'
import ReactDOM from 'react-dom/client'

export default function MyComponent() {
  return <div></div>
}

ReactDOM.createRoot(document.getElementById('react')).render(<MyComponent />)